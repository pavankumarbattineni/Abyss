# Abyss
Abyss is a no-code AI agent builder featuring a production multi-agent backend with parallel sub-agent orchestration via LangGraph's Send API, token-by-token SSE streaming with reconnect support, recurring agent scheduling, and MCP tool integrations with Fernet-encrypted credential storage and PostgreSQL-backed persistence.
