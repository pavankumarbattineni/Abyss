# Web framework
fastapi>=0.116.0
uvicorn[standard]>=0.35.0
gunicorn>=22.0.0

# Data validation
pydantic>=2.7.4
pydantic-settings>=2.0.0

# HTTP client
httpx>=0.27.0

# MCP (floor bumped — langchain-mcp-adapters 0.3.0 requires mcp>=1.9.2;
# ceiling capped — mcp 2.0.0 renamed RequestContext out of mcp.shared.context,
# breaking langchain-mcp-adapters 0.3.1's import of it)
mcp>=1.9.2,<2.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0

# Security
firebase-admin>=6.5.0
bcrypt>=5.0.0
cryptography>=42.0.0
PyJWT>=2.9.0

# LangChain / LangGraph ecosystem
# All packages below align on langchain-core>=1.4.7,<2.0.0
langchain-core>=1.4.7
langchain-openai>=1.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langchain-groq>=1.1.0
langchain-mcp-adapters>=0.3.0
langgraph>=1.2.0
langgraph-checkpoint-postgres>=3.0.0

# Token usage / cost calculation — standardized per-provider pricing so we
# don't maintain our own pricing table (see services/chat_service.py).
# LITELLM_LOCAL_MODEL_COST_MAP=True (set before import) makes pricing lookups
# use only the bundled local data file — no network call at startup or per-request.
litellm>=1.96.0

# Testing
# Pinned below 9 to avoid conflicts with langchain-tests / syrupy if installed
pytest>=8.3,<9
pytest-asyncio>=0.24.0
pydantic[email]>=2.7.0,<3.0.0
